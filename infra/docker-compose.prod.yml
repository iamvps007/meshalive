services:
  api:
    restart: unless-stopped
    deploy:
      resources:
        limits:
          memory: 512M
  web:
    restart: unless-stopped
    deploy:
      resources:
        limits:
          memory: 512M
  postgres:
    restart: unless-stopped
  redis:
    restart: unless-stopped
  nginx:
    restart: unless-stopped
